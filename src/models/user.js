class User {
  constructor({ name, city, country, favoriteSport }) {
    this.name = name
    this.city = city
    this.country = country
    this.favoriteSport = favoriteSport
  }
}

module.exports = User